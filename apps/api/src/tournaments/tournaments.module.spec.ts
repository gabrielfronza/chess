import { TournamentsModule } from './tournaments.module';

describe('TournamentsModule', () => {
  it('defines the tournament administration module', () => {
    expect(TournamentsModule).toBeDefined();
  });
});
